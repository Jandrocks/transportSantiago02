import s from "./style.module.css";
const TitleStationBus = ({ titleStation}) => {   
  return (
    <div className={s.title_station}>{titleStation}</div>
  )
}

export default TitleStationBus

  
  
